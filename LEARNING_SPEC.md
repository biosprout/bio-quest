# BIO QUEST 学習状態・復習スケジューラ仕様（第1段改修）

対象: `index.html`（APP_VER `20260908a` 以降）。問題データの仕様は `CONTENT_SPEC.md`。

目的は、智穂子がキャンベル級の生物学を広く・深く身につけられるように、
**問題ごとの復習管理を裏側で持ち、「今日の10問」の中身をそれに接続する**こと。
画面・達成記録・レベル・分野構成は変えない。

---

## 1. 変えないもの

- 問題の内容・ID・7項目 schema（`id,f,lv,q,ch,a,ex`）。原本は `index.html` 内の `const Q`。
- `state.stats[id] = {c, w, d?, wd?}` の書き方と意味。実績・図鑑・称号・「クリア」・苦手判定は従来どおり **`state.stats` だけ** で判定する。
- 「今日の10問」の名称・カードの位置と見た目・同日セットの保持・途中再開・完了判定・報酬（`dailyDone`、chime、XP）。
- ホームの構成、他モード（分野べつ／ミックス／タイムアタック／苦手だけ復習）。

内部の学習段階・定着率・復習の滞留数・休んだ日数ぶんのノルマは **画面に出さない**。

## 2. 内部学習状態

```
state.learn = { v:1, since:'YYYY-MM-DD', q:{ [id]: L } }
L = {
  s  : 段階 1=挑戦済み 2=別日再確認 3=定着の目安 （未挑戦は記録なし）
  n  : 記録開始後の回答回数
  cd : 正解した「学習日」の数（同じ日に何度正解しても 1）
  k  : 復習間隔の段（LEARN.STEPS の添字。-1 = まだ間隔なし／誤答直後）
  ld : 最後に回答した学習日        lcd : 最後に正解した学習日
  la : 最後に回答した時刻 (ms)     lr  : 最後の正誤 1/0
  due: 次に見直す学習日（null = 予定なし）
  lp : 記録開始後の誤答回数
  mig: 1 なら、更新前（state.stats）に回答記録があった問題
}
```

段階は `learnStage(q)` で得る: 記録 `L` があれば `L.s`、なければ `state.stats[id]` があれば 1（挑戦済み）、どちらもなければ 0（未挑戦）。

### 学習日の境界

学習日 = **端末のローカル時刻の暦日（00:00 で切り替わる）**。既存の `dayNum()` と同じ定義で、
「今日の10問」の日替わり（`todayStr()`）・連続日数と同じ境界。智穂子の端末では JST。
ヘッドレス検証では `Asia/Tokyo` で 23:59:30 → 翌 00:00:30 をまたいだとき（UTC では同じ日）に
学習日が 1 進むことを確認している。

### 遷移ルール（`learnRecord(q, ok)`、回答 1 回につき 1 回だけ呼ぶ）

調整用の定数は `index.html` の `LEARN`:

| 定数 | 暫定値 | 意味 |
|---|---|---|
| `STEPS` | `[1,3,7,14,30,60]` | 別の日に正解するたびに段が 1 つ進み、次回までの日数がこれになる（上限 60 日で維持） |
| `LAPSE_DAYS` | `1` | 誤答したら段を `-1` に戻し、翌日に再確認 |
| `STABLE_DAYS` | `3` | 定着の目安の条件その 1: 正解した日が 3 日以上 |
| `STABLE_IVL` | `14` | 定着の目安の条件その 2: 次回間隔が 14 日以上（= `STEPS` の 14 に到達） |

- 正解で **その学習日はじめての正解** のとき: `cd++`、`k = min(k+1, 5)`、`due = 今日 + STEPS[k]`。
- 正解でも同じ学習日の 2 回目以降: `n / la / lr / ld` だけ更新し、**段も cd も due も進めない**。
- 誤答: `lp++`、`k = -1`、`due = 今日 + LAPSE_DAYS`（翌日）。翌日に正解すると `k = 0` で **さらに翌日**、その次の別日正解で `+3` 日と、再学習は 2 日連続で確かめてから間隔を開ける。
- 段階の判定: `cd >= STABLE_DAYS かつ k >= 0 かつ STEPS[k] >= STABLE_IVL` なら 3、`cd >= 2` なら 2、それ以外は 1。
  - 一度の正解や同日中の反復だけでは 2 以上にならない。定着の目安 (3) に届くのは、実質「別の日の正解が 4 回目（1 → 3 → 7 → 14 日）」のとき。
  - 誤答で `k = -1` になるので、3 だった問題は 2 に戻る（`cd` は保持）。内部状態は戻るが、`state.stats` と実績には影響しない。
- どのモードの回答も同じ扱い（分野べつ・ミックス・タイムアタック・苦手復習も復習として数える）。時間切れは誤答。

### 二重記録の防止

`answer()` は `g.answered` で 1 問につき 1 回しか本体を実行しない。`learnRecord` はその中で `state.stats` の更新と同時に呼ばれ、直後に `save()` する。
再読み込み・中断で失われるのはメモリ上のクイズ進行だけで、保存済みの記録が二重になることはない
（同日中の再挑戦は記録されるが、段は進まない）。

## 3. 旧履歴の扱い（移行）

- **遅延方式**。更新時に `state.learn = {v:1, since, q:{}}` を作るだけで、旧記録は `state.stats` に残したまま読み取り専用で参照する。`L` は更新後に初めて解いたときに作る（`mig:1` を付ける）。
- 過去の回答日時・「別の日に正解した」事実は捏造しない。旧記録しかない問題は段階 1（挑戦済み）で `due` なし。
- したがって旧問題が一斉に「期限超過」になることはなく、復習枠が余った日に「既出の再確認」として少しずつ混ざる。
- `ensureLearn()` は何度実行しても状態を変えない（欠けているキーを補うだけ）。旧形式の state を繰り返し読み込んでも `state` は同一。
- 実績・XP・クリア数・カード・連続日数は一切触らない。

## 4. 「今日の10問」の選問（`buildDaily()`）

対象は従来どおり `pool('all', curLv)`（難易度のしぼり込みは維持）。今日すでに解いた問題は候補から外す（最後の手段としてだけ使う）。

| 区分 | 定義 | 並べ方 |
|---|---|---|
| 未挑戦 `fresh` | `state.stats` にも `learn` にもない | 順不同 |
| 復習 `due` | `L.due <= 今日` | `k` が小さい順（誤答直後 → 間隔の短いもの）→ `due` が古い順 → 乱数 |
| 既出の再確認 `fill` | 解いたことはあるが今日は予定がない | 苦手（`w>0 && c<=w`）→ 予定のない旧問題 → 先の予定が入っているもの、の順。その中で長く見ていないもの優先 |

配分（`DAILY_N=10, DAILY_NEW_BASE=5, DAILY_NEW_MIN=2`）:

1. `nNew = min(fresh, 5)`。復習が 5 問を超える日は `nNew = min(fresh, max(2, 10 − due))` に減らす。
2. `nRev = min(due, 10 − nNew)`。
3. 10 問に足りなければ、**既出の再確認 → 未挑戦の追加 → 復習の追加 → 今日解いた問題** の順で補う。
4. 同じセットに同じ問題は入れない。最後に順番をシャッフル。

例: 復習 0 なら「未挑戦 5 + 再確認 5」、初日で未挑戦しかなければ「未挑戦 10」、復習 12 なら「未挑戦 2 + 復習 8」、復習 6 なら「未挑戦 4 + 復習 6」、未挑戦が尽きて復習 0 なら「再確認 10」。

セットの保持（`state.daily = {d, lv, ids, ok, ng, fin}`）、始める前の難易度変更で作り直す挙動、完了時の `dailyDone`・chime・XP は従来のまま。

## 5. 保存・復元

- `exportProgress()` は `v:2` になり、`d.learn` と `d.dailyDone` を含む（`dailyDone` は従来書き出されておらず、`daily7/daily30` の判定元だったので合わせて保存する）。
- `importProgress()` は `d.learn` があれば復元し、なければ空で作り直す（**v1 の旧バックアップもそのまま読める**）。`d.dailyDone` も復元する。
- 学習履歴の外部送信・サーバーはない。教材更新（APP_VER の更新バー → `forceUpdate()` の再読み込み）は `localStorage` に触らない。Service Worker は使っていない（従来どおり）。

## 6. 端末のコンソールで状態を見る

```js
learnSummary()        // {unseen, seen, review, stable, dueToday, legacyOnly, tracked, since}
learnOf('c_e1')       // 1 問の記録 L（なければ null）
state.daily           // 今日のセット
```

いずれも画面には出さない。数字を智穂子に見せる必要はない。

## 7. 検証

`scripts/test-learn.mjs`（Playwright、`Asia/Tokyo`、偽の時計）で 53 項目:
初回 10 問／全問正解後の記録／同日反復で段が進まない／同日誤答／JST の日付境界／翌日の 2+8 配分／
別日正解で段階 2／定着の目安 3 と誤答での後退／中断・再読み込み・再開（二重記録なし）／
export・import（v1 と v2）／旧履歴の遅延移行（期限超過ゼロ・冪等・実績不変・苦手優先）／
対象不足（未挑戦 0・復習 12・復習 6・復習 3・IBO 級のみ）／他モードの記録／APP_VER。
別スクリプトで、旧アプリ（`20260903i`）が `learn` 入りの state を読んでも壊れないこと、`?v=` 付き再読み込みで記録が残ることを確認。

端末でしか確認できないもの: ホーム画面アイコンからの起動での更新バー表示、Safari の実時計での日付切り替え、機種変更を伴う復元。

## 8. 戻し方

このコミットを revert して `APP_VER` を上げるだけでよい。旧アプリは `state.learn` を無視して従来どおり動く（`state.stats` は形式を変えていない）。
`state.learn` を消したい場合は端末のコンソールで `delete state.learn; save();`。実績・クリア数には影響しない。
